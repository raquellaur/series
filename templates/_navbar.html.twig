<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#"><img src="{{ asset('build/images/logo.jpg') }}" width="60px" alt=""/></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_index') }}"> Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('program_index') }}"> Programs <span class="sr-only">(current)</span></a>
            </li>
            {% include 'category/_navbar.html.twig' %}
            <form class="form-inline">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
        </ul>
        <ul class="navbar-nav flex-row ml-md-auto d-none d-md-flex">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link my-2 my-sm-0" href="{{ path('app_logout') }}"> Se déconnecter</a>
                </li>
                <span class="navbar-text mr-sm-3">
                    {{ app.user.email }}
                </span>
                <li class="nav-item">
                    <a class="nav-link my-2 my-sm-0" href="{{ path('app_account', {id: app.user.id}) }}">My Account</a>
                </li>
            {% else %}
                <li class="nav-item my-2 my-sm-0">
                    <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
                </li>
                <li class="nav-item my-2 my-sm-0">
                    <a class="nav-link" href="{{ path('app_register') }}"> Se inscrire</a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>